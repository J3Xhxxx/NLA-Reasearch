# NLA × SAE 项目恢复与交接（N6+ 完成后）

> 最后更新：2026-08-03（Asia/Shanghai）  
> 当前最新事实来源：
>
> 1. `results/N6_FINAL_ANALYSIS_2026-08-03.md`
> 2. `results/n6_pull_staging/n6_pull_20260803T061302Z/n6_analysis_v1.json`
> 3. `results/n6_pull_staging/n6_pull_20260803T061302Z/n6_independent_audit_v1.json`
> 4. `results/N5_ANALYSIS_V2.md`
> 5. `results/N4_REAL_CONTENT_CAUSAL_AUDIT_2026-07-30.md`
>
> `RECOVERY_2026-08-01.md` 和 `continue.md` 的正文保留历史价值，但其中
> “N6 尚未执行”“下一项 GPU 是 N6”及旧服务器状态已经过时。

## 1. 当前一句话状态

N6+ 已在全新 400-row fresh-Pile cohort 上完成预注册的 matched candidate-content
causal specificity 实验。H6-A 与 H6-B 均通过，独立 stage 56 逐端点复算通过，
所以允许正式使用：

**`SAMPLE-SPECIFIC NATURAL-LANGUAGE PREDICTIVE-STATE CODE CONFIRMED`**

服务器结果已回传并校验，随后已发送
`sync; /usr/bin/shutdown -h now`；两次 SSH 复查均不可连接。不要为确认而重新开机，
控制面最终状态由用户在 AutoDL 页面查看。

## 2. N6+ 正式裁决

### 2.1 Confirmatory positive

| Endpoint | Result |
|---|---:|
| `G_specific` | `+0.117954`, 95% CI `[+0.102860,+0.133995]` |
| raw `KL_cross-KL_true` | `+1.827883 nat/row`, CI `[+1.591758,+2.074946]` |
| `G_content` | `+0.154382`, CI `[+0.136602,+0.173507]` |
| `T_p3` | `0.995175`, one-sided 95% lower `0.993194` |
| `A_meanmass` | `+9.008470`, CI `[+8.444855,+9.572597]` |

正式标签：

- `H6-A: SAMPLE-SPECIFIC CHANNEL CONFIRMED`
- `H6-B: PREDICTIVE ALIGNMENT CONFIRMED`
- headline pass

这排除了当前最重要的替代解释：p3 的作用不是仅由 candidate-list 格式、
generic lexical density 或熟悉表达造成。属于 recipient 自己的 candidates
提供了显著、可重复的增量因果信息。

### 2.2 Secondary nuance

- `M_majority=+0.040763`，CI `[+0.028680,+0.053252]`：
  `MAJORITY-OF-CANDIDATE-BENEFIT SUPPORTED`。
- `G_candidate_anchor=-0.045042`，CI `[-0.078669,-0.012779]`：
  `NO CANDIDATE DOMINANCE CLAIM`。

因此不得把机制写成“候选支配 p3”。只去 anchors 的 recovery `.77534`，
比只去 candidates 的 `.82039` 更低；全部 quoted content 去掉后只有 `.36707`。
当前证据支持 anchors 与 sample-specific candidates 的互补结构。

### 2.3 Comparator boundary

Aggregate recovery：

- `orig=.97949`
- `sae_big=.97814`
- `p3_true=.97477`
- `p3_cross_matched=.85681`
- `p12=.79553`

N6 没有为 NLA–SAE superiority/equivalence 预注册 gate，所以数值接近不能写成
“NLA 优于 SAE-big”“安全 parity”或“SAE 可被替代”。冻结的 N5 centered-cosine
router 仍是 `H5-A: NO SELECTIVE CLAIM`。

## 3. Cohort 与审计

- provisional：501；
- AV-format eligible：434（86.63%）；
- frozen analysis：400 rows / groups / documents；
- 13 个 Pile sources，每个 22–32 行；
- 4/5/6 candidates：105/192/103 行；
- 全部 N4/N5 document、group 和 20-word shingles 被 embargo；
- cross donor 同 source、同 candidate count，one-to-one derangement，
  且没有共享规范化候选；
- 50,000 ordinary row bootstraps，`PCG64(20260803)`；
- stage 56 的 56 个 endpoint leaves 全部与 stage 55 一致至 `1e-12`；
- 所有正式 label 逐字一致；
- identity KL 完全为 0；仅一项 `-2.20e-9` 浮点 roundoff 被依约记录并钳零。

Mandatory diagnostics 也支持稳健性：

- 13/13 sources 的三个主效应点估计均为正；
- leave-one-source-out 结果全部稳定为正；
- true/cross candidate hit@1 为 66.5% / 3.75%；
- observed next token membership 为 49.0% / 2.75%；
- 210/400 行满足 `KL_cross-KL_true > 1 nat`。

## 4. N1–N6 后最稳妥的论文主线

> Activation reconstruction through language must be evaluated separately at
> the levels of centered geometry, textual content, downstream causal fidelity,
> and tail safety. For Gemma-3-12B-IT L32, the released NLA carries a
> sample-specific, next-token-aligned predictive-state code whose downstream
> causal utility is concentrated in the final paragraph. This does not yet
> establish proposition-level human faithfulness, global superiority over
> SAE-big, or a safe selective router.

论文可以同时报告：

1. N6 confirmatory positive：matched candidate identity/content 有因果特异性；
2. N5 confirmatory positive：p3 channel 在新 cohort 上 near-sufficient；
3. N5 confirmatory negative：简单 centered-cosine router 没有建立；
4. 方法论结论：geometry、closed-loop decodability 与 downstream causal fidelity
   不能互相替代；
5. N3 诊断修正：小 synthetic corpus 的 zero activation 是 coverage failure，
   不能证明 feature death。

论文仍然禁止声称 human-faithful explanation、NLA global superiority、safe routing、
candidate dominance、cross-layer/model generality 或 steering。

## 5. 本地产物

Verified pull：

`results/n6_pull_staging/n6_pull_20260803T061302Z/`

其中：

- 51 个文件，共 45,583,606 bytes；
- 15 个 SHA-256 sidecars 全部验证通过；
- stage 49–56 与 supervisor exit markers 全为 0；
- main analysis：
  `n6_analysis_v1.json`
- independent audit：
  `n6_independent_audit_v1.json`
- human-readable result：
  `N6_ANALYSIS_V1.md`
- frozen preregistration：
  `n6_plus_preregistration_v2.md`
- frozen code manifest：
  `n6_code_manifest_v2.txt`
- resource report：
  `n6_resource_report_v1.txt`

Stage-50 activation parquet 与 metadata 位于持久化数据盘的远端 `activations/`，
不属于冻结 `n6_*/N6_*` pull allowlist，因此目前未在本地。它们不是正式分析或
独立审计的必需输入，也不需要重跑；未来只在无 GPU/无卡模式下补拉。

## 6. 运行与关机事实

- v2 supervisor：12:16:54–14:11:45，wall time `1:54:51`；
- AV：501 rows，约 58.4 分钟；
- AR：约 125 秒模型计算；
- causal：4400 forwards，约 393 秒；
- pipeline exit：0；
- 结束快照：A800 80 GB，0 MiB / 0%；
- 结果在发送 ACK 前已全部下载并校验。

本地 monitor 在 12:59 因 SSH reset 提前退出。root 在 pull-ready 后手动执行了相同的
fail-closed 协议并在 300 秒窗口内写入 exact ACK。未来任何新 launcher 必须在冻结前修复：

1. polling 对短暂 SSH reset 应有 bounded retry；
2. pull-ready key parser 不能用只接受 `[a-z_]` 的表达式，因为
   `analysis_sha256` 等 key 含数字；
3. 如需完整 cache 归档，应显式加入 activation artifact allowlist。

不要修改 N6 v2 frozen scripts 后再声称原 manifest 仍能验证；新修复应另建版本和新 manifest。

## 7. 下一步

### 现在，不开 GPU

1. 更新 `results/PROJECT_CLAIM_TABLE_2026-08-03.md` 到 N6 后状态；
2. 起草 paper Methods、N4–N6 Results、Limitations 与 artifact appendix；
3. 从 frozen JSON 生成主图与表格；
4. 做 N4/N5/N6 CPU-only tail forensics，明确标为 discovery。

### 下一次 GPU

优先补最主要的外推缺口，而不是同层重复 N6：

1. 若有兼容的第二层 NLA AV/AR checkpoint，做预注册 cross-layer N6 replication；
2. 否则做第二模型/checkpoint replication；
3. 再考虑 candidate-semantic paraphrase robustness；
4. 若要比较 NLA 与 SAE，必须做 capacity/rate-matched operating curve。

在检查 checkpoint 可用性、磁盘、统计 power、冻结 protocol 和可靠关机方案前，
不要启动新 GPU。

## 8. 新模型安全启动顺序

1. 先读本文件与 `results/N6_FINAL_ANALYSIS_2026-08-03.md`；
2. 数值以 frozen JSON 和 independent audit 为准；
3. 把 `RECOVERY_2026-08-01.md`、`continue.md`、`README.md` 等旧正文当历史；
4. 不重跑 N6，不事后重定义 endpoints；
5. 新实验必须有新 preregistration、code manifest、input/model hashes；
6. 先补 launcher bounded retry 与 key-parser bug，并保存 N6 v2 原始版本；
7. GPU 批次完成后，必须先 pull + SHA-256 verify，再
   `sync; /usr/bin/shutdown -h now`；
8. `systemctl poweroff` 在该 AutoDL 容器环境不可用。
